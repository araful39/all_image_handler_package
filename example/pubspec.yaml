name: example
description: "Example app for all_image_handler"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.11.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  all_image_handler:
    path: ../

cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/